
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phone
    username: account
    password: account123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
springdoc:
    api-docs:
          path: /api-docs          